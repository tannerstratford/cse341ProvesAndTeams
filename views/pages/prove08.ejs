<!DOCTYPE html>
<html>

<head>
  <title>JSON data</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
    integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <link href="/css/results.css" rel="stylesheet" type="text/css" />
</head>

<body>
  <div class="container">
    <h1>JSON data</h1>
    <% for (var i= itemCount; i < parseInt(itemCount) + 10 && i < data.length; i++) { %>
      <% if(i % 3==0) { %>
        <div class="card-deck">
          <% } %>
            <div class="card cCard">
             <img src="<%= data[i].imageUrl %>" class="card-img-top cImage"> 
              <div class="card-body">
                <h5 class="card-title">
                  <%= data[i].name %>
                </h5>
                <h6>
                  <%= data[i].price %>
                </h6>
                <p class="card-text cText">
                  <%= data[i].description %>
                </p>
              </div>
            </div>
            <% if(i % 3==2) { %>
        </div>
        <br>
        <% } %>

          <% } %>
        </div>
          <div class = "container">
          <div style = "display: inline-block;">
          <form action="/prove08/0">
            <button type = submit>First Page</button>
          </form>
          </div>
          <div style = "display: inline-block;">
          <%if(itemCount < parseInt(data.length) - 10) { %>
            <form action="/prove08/<%=parseInt(itemCount) + 10%>">
              <button type = submit>Next Page</button>
            </form>
            <% } %>
          </div>
          <div style = "display: inline-block;">
            <%if(itemCount >= 10) { %>
              <form action="/prove08/<%=parseInt(itemCount) - 10%>">
                <button type = submit>Previous Page</button>
              </form>
              <% } %>
          </div>
          <div style = "display: inline-block;">
            <form action="/prove08/<%=parseInt(data.length) - 10 %>">
              <button type = submit>Last Page</button>
            </form>
          </div>
        </div>
        
             
          
  
</body>

</html>
